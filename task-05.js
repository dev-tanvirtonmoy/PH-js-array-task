// Task Brief
// Combining Arrays
    // Create two arrays of your choice.
    // Use the concat method to combine the two arrays into a new array.
    // Print both the original arrays and the combined array using console.log().

const array1 = ['Bobby', 'Tonmoy', 'Tanvir']
const array2 = [22, 12, 26]
console.log(array1)
console.log(array2)
const arrayMarged = array1.concat(array2);

console.log(arrayMarged);
